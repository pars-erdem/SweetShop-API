# SweetShop-API
This project is a basic API for managing the stock of a dessert shop, developed using Django REST Framework. The API efficiently manages dessert inventories, allowing you to track stock levels, add new items, update existing items, and delete items from the inventory.
